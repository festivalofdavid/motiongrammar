% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initiate.R
\name{initiate_manual_csv}
\alias{initiate_manual_csv}
\title{Manually Parse CSV Files with User-Specified Structure}
\usage{
initiate_manual_csv(
  .data_path,
  skip = 0,
  header_row = NULL,
  col_unix,
  col_lat,
  col_lng,
  col_altitude = NULL,
  coord_system = c("gps", "local"),
  n_max = NULL,
  comment = "#",
  delimiter = ","
)
}
\arguments{
\item{.data_path}{String. Path to the CSV file.}

\item{skip}{Integer. Number of lines to skip before the header (default: 0).}

\item{header_row}{Integer. Row number containing column headers. If NULL, uses skip + 1.}

\item{col_unix}{String. Name of the unix time column (case-insensitive).}

\item{col_lat}{String. Name of the latitude/y column (case-insensitive).}

\item{col_lng}{String. Name of the longitude/x column (case-insensitive).}

\item{col_altitude}{String. Name of the altitude/z column (case-insensitive). Set to NULL if not present.}

\item{coord_system}{String. Output coordinate system: "gps" or "local" (default: "gps").}

\item{n_max}{Integer. Maximum number of rows to read. NULL reads all rows (default: NULL).}

\item{comment}{String. Comment character to ignore. Lines starting with this are skipped (default: "#").}

\item{delimiter}{String. Field delimiter (default: ",").}
}
\description{
Parses CSV files when the user knows the exact structure.
Useful for problematic files where auto-detection fails.
}
\keyword{internal}
