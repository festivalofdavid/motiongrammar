% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantitate.R
\name{quantitate}
\alias{quantitate}
\title{Quantitate Motion Grammar}
\usage{
quantitate(
  .data,
  allocation = "allocation_1",
  derivative = "velocity",
  scope = "designation",
  active_only = FALSE,
  ...
)
}
\arguments{
\item{.data}{Tibble output from \code{allocate()}.}

\item{allocation}{Character; the allocation name used in \code{allocate()}
(e.g. \code{'allocation_1'}). Set to \code{NULL} to skip band grouping
and compute totals within designation segments (or the whole session).}

\item{derivative}{Character; one or more derivatives whose bands to group
by. Any combination of \code{'velocity'}, \code{'acceleration'},
\code{'angular_velocity'}. Ignored when \code{allocation = NULL}.}

\item{scope}{Character; controls grouping and row inclusion:
\describe{
\item{\code{'designation'}}{(default) Groups by \code{designation} and
bands. Use \code{active_only} to optionally restrict to active
segments.}
\item{\code{'session'}}{Groups by bands only across the whole session
(active and relief rows included). Use this to get overall session
totals per band.}
\item{\code{'active'}}{Groups by bands only, restricting to rows whose
\code{designation} starts with \code{'active_'}. Relief periods are
excluded.}
}}

\item{active_only}{Logical; when \code{scope = 'designation'}, restricts
rows to segments whose \code{designation} starts with \code{'active_'}.
Ignored for other scopes. Defaults to \code{FALSE}.}

\item{...}{Summarise expressions passed to \code{dplyr::summarise()}.}
}
\value{
A summarised tibble. Grouping columns are \code{designation} (when
\code{scope = 'designation'}) and/or the resolved band columns (when
\code{allocation} is provided). The full pipeline quality log is attached
as \code{attr(result, 'quality')} so the output is self-describing.
\strong{Note:} R attributes are not preserved by CSV export
(\code{write.csv}, \code{readr::write_csv}, etc.). Save as RDS
(\code{saveRDS}) or call \code{attr(result, 'quality')} before writing
to preserve provenance.
}
\description{
Aggregates motion data by designation and one or more intensity
band dimensions. Three scopes control what rows are included and how the
designation column is used in grouping. Set \code{allocation = NULL} for
band-free totals.
}
