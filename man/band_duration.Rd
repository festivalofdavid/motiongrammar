% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantitate.R
\name{band_duration}
\alias{band_duration}
\title{Time in Bands}
\usage{
band_duration(
  .data,
  allocation = "allocation_1",
  derivative = "velocity",
  scope = "designation",
  active_only = FALSE
)
}
\arguments{
\item{.data}{Tibble output from \code{allocate()}.}

\item{allocation}{Character or \code{NULL}; allocation name. \code{NULL}
returns duration per designation segment (or whole session).}

\item{derivative}{Character; one or more derivatives to group by. Ignored
when \code{allocation = NULL}.}

\item{scope}{Character; \code{'designation'}, \code{'session'}, or
\code{'active_session'}. See \code{\link{quantitate}}.}

\item{active_only}{Logical; passed to \code{\link{quantitate}} when
\code{scope = 'designation'}.}
}
\value{
A summarised tibble with a \code{duration_sec} column.
}
\description{
Convenience wrapper around \code{\link{quantitate}} that
computes the duration spent in each band (or designation segment when
\code{allocation = NULL}). Requires interpolated data so that each row
represents a fixed time interval, derived from \code{interpolation_hz}
in metadata.
}
